<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FullApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SimpleListViewActivity" />
        <activity android:name=".ArrayAdapterActivity" />
        <activity android:name=".CustomListViewActivity" />
        <activity android:name=".BaseAdapterListViewActivity" />

        <activity android:name=".DialogActivity" />

        <activity android:name=".ProgressBarActivity" />

        <activity android:name=".StudentActivity" />

        <activity android:name=".StudentDetailActivity" />

        <activity android:name=".adapter.FragmentDemoActivity"/>

        <activity android:name=".database.StorageDemoActivity" />

        <activity android:name=".database.SqliteActivity" />

        <activity android:name=".room.RoomActivity" />



    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />




</manifest>