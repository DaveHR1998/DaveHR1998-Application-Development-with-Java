<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- App-specific Storage -->
        <Button
            android:id="@+id/btnSaveInternal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save to App-Specific Storage" />

        <Button
            android:id="@+id/btnReadInternal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Read from App-Specific Storage" />

        <!-- Shared Storage -->
        <Button
            android:id="@+id/btnSaveShared"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save to Shared Storage" />

        <!-- SharedPreferences -->
        <Button
            android:id="@+id/btnSavePref"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save to SharedPreferences" />

        <Button
            android:id="@+id/btnReadPref"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Read from SharedPreferences" />

        <TextView
            android:id="@+id/txtOutput"
            android:textSize="16sp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="16dp"/>
    </LinearLayout>
</ScrollView>
